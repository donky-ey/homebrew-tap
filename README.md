# donky-ey/homebrew-tap

Homebrew tap for [PokeDexBar](https://github.com/donky-ey/PokeDexBar) — a macOS menu bar app
that turns the AI coding tokens you already spend into a Pokémon collection.

```bash
brew install --cask donky-ey/tap/poke-dex-bar
```

Requires macOS 14 (Sonoma) or later.
